>>>>>>> main/main
# -*- mode: yaml -*-

name: Build conda package
on:
  workflow_dispatch:

jobs:
  build:
    name: Disabled build
    runs-on: ubuntu-slim
    if: false
    steps:
    - run: exit 0
